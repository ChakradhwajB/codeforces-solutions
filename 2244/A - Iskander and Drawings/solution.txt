t = int(input())
 
 
for _ in range(t):
    n = int(input())
    arr = str(input())
 
    index = []
    current_len = 0
    max_len = 0
    for char in arr:
            if char == '#':
                current_len += 1
                if current_len > max_len:
                    max_len = current_len
            else:
                current_len = 0
 
    if max_len == 0:
        print("0")
    else:
        print(str((max_len + 1) // 2))